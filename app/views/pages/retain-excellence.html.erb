<% content_for :subsection do %>
  <div class="subsection" id="subsection_retain_excellence">
  <h1>RETAIN EXCELLENCE</h1>
  <div class="commitment_subtabs"><%= Content.insert 'retain_excellence' %></div>
  <% Organization.all_partners.where(:commitment_category=>'retain_excellence').order(:name).each do |organization| %>
    <%= render :partial=>'commitment', :locals=>{:organization=>organization} %>
  <% end %>
  </div>
<% end %>
<%= render file: 'pages/commitments', locals: { heading: 'Retain Excellence' } %>